# Gemini 多頁工作區架構檢視與第三單元擴充建議

## 現有架構評估

`gemini-pages/` 已採用適合逐檔修改的靜態架構。每個獨立 HTML 練習頁只負責指定 `data-unit`、`data-module`、中英文標題及對應資料檔；共用視覺樣式集中在 `assets/lesson.css`；答題、語音、字數統計與本機進度集中在 `assets/lesson.js`；每個 S2 單元則以一個明確的 JavaScript 資料檔保存六個技能區塊。這個分工可避免 Gemini 為了改一題詞彙而改動整個使用者介面。

| 層級 | 目前責任 | 第三單元可沿用方式 |
|---|---|---|
| `gemini-pages/index.html` | 顯示可開啟的獨立練習頁目錄。 | 新增一個 S2 Action 區段及六張技能卡。 |
| `gemini-pages/s2/*.html` | 每頁選擇一個單元及技能，不儲存題目。 | 複製對應 S2 Connect 頁，再更改單元、標題和資料檔。 |
| `gemini-pages/data/*.js` | 儲存可讀、可逐題修改的雙語資料。 | 新增 `s2-community-and-environment-data.js`。 |
| `gemini-pages/assets/lesson.js` | 題目渲染、四選一、語音、寫作字數及本機進度。 | 由硬編碼條件改為單元登記表，讓第三單元及日後單元只需新增一列設定。 |
| `.gemini-pages-audit.js` | 檢查頁面、資料檔、技能數量及基本結構。 | 擴充為由登記表檢查所有單元，避免手動重複規則。 |

## 已識別的擴充重點

目前共用程式使用 `if` 判斷以辨識兩個 S2 單元，並另外以條件式決定標籤和資料檔名稱。此方法在兩個單元時清楚，但第三個及更多單元會令程式逐漸重複。因此，第三單元會把這些條件改為一個小型 `UNIT_REGISTRY`。每個單元只需登記全域資料名稱、資料檔名稱、顯示標籤和學習焦點；新增單元時不用修改多段渲染程式。

> **安全修改原則：** 題目、選項、答案、繁體中文支援和示範語句應只在單元資料檔修改。只有改變所有獨立頁的共同互動行為時，才修改 `assets/lesson.js` 或 `assets/lesson.css`。

## 第三個 S2 單元建議

第三單元定為 **S2 Action: Community and Environment｜中二行動：社區與環境**。它由前兩個單元的「個人經驗與選擇」及「訊息、資料來源與受眾」進一步轉到可實行的社區行動。學生會比較可持續做法、描述物品和地點、理解規則與數量、使用行動建議，並以證據提出改善方案。

| 範疇 | 主要語言重點 | 原創互動內容 |
|---|---|---|
| 文法 | `there is/are`、`some/any`、`much/many/few/little`、`too/enough`、目的不定詞、第一類條件句。 | 16 題社區、學校及環境語境選擇題。 |
| 詞彙 | 可持續生活、社區設施、資源、參與及改善行動。 | 18 題附英語定義、例句及繁體中文支援的四選一詞彙題。 |
| 閱讀 | 以文本目的、證據、步驟及比較為焦點。 | 4 組配對文本、12 題問題。 |
| 聆聽 | 公告、會議及訪問中的行動、原因、時間及數量。 | 3 段可重播腳本、12 題問題及聽後逐字稿。 |
| 寫作 | 說服、建議及提出改善方案。 | 3 項 100–120 字任務及自我檢核。 |
| 口語 | 推薦行動、比較方案及報告結果。 | 3 項 45–60 秒任務、英文示範及非評分式自檢。 |

## 日後加入第四單元的建議

第四單元可聚焦 **S2 Perspective: Identity and Stories｜中二觀點：身分與故事**，以敘事時態、形容詞從句、人物觀點、同一事件的不同敘述及訪談口語為核心。當完成三個 S2 單元後，建議再加入一個小型單元總覽頁，讓學生按「文法、閱讀策略、寫作體裁或社區主題」選擇跨單元複習，而不是立即複製更多介面。

## Gemini 修改提示

建立第三單元後，可使用以下指令讓 Gemini 進行低風險內容修改：

> 「只修改 `gemini-pages/data/s2-community-and-environment-data.js`。新增四題原創 S2 詞彙題，主題是香港社區環境行動。保留 7 欄詞彙格式、四個選項、英文優先、繁體中文支援，並不要修改 HTML、CSS 或共用 JavaScript。」

> 「只修改 `gemini-pages/s2/s2_action_read_community_environment.html`。保留 `data-unit`、`data-module` 及兩個共用資源連結；只更新頁面標題與說明文字。」

所有 S1／S2 題目均應保留 **Original Practice｜原創練習** 標籤，並不得表述為官方考試試題。
